import { DefaultTheme } from "styled-components";

const Theme: DefaultTheme = {
  blueColor: "#3498db",
  borderRadius: "5px",

  colors: {
    main: "cyan",
    secondary: "magenta"
  }
};

export { Theme };
